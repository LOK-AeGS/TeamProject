
spring:
  mvc:
    hiddenmethod.filter.enabled=true
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MariaDB10Dialect
    hibernate.ddl-auto: create
    show_sql: true

  datasource:
    url: jdbc:mariadb://localhost:3306/MallProject
    username: root
    password: Woodpecker1324@
    driver-class-name: org.mariadb.jdbc.Driver



